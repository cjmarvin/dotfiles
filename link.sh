# GIT
echo '[user]' > ~/.gitconfig.local
echo '    name = ' >> ~/.gitconfig.local
echo '    email = ' >> ~/.gitconfig.local
vim ~/.gitconfig.local
#ln -sv `pwd`/git/gitconfig ~/.gitconfig
# ln -sv `pwd`/git/gitignore ~/.gitignore

# TMUX
# ln -sv `pwd`/tmux/tmux.conf ~/.tmux.conf

# VIM
# ln -sv `pwd`/vim/vim ~/.vim
# ln -sv `pwd`/vim/vimrc ~/.vimrc

# ZSH
# ln -sv `pwd`/zsh/.zshenv ~/.zshenv
# ln -sv `pwd`/zsh ~/.zshrc
# ln -sv `pwd`/zsh/zshrc ~/.zshrc
