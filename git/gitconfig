[include]
    path = .git-user
[core]
	editor = vim
    whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
	excludesfile = /Users/starvinmartian/.gitignore
[color]
	ui = auto
	# interactive = auto
    [color "branch"]
        current = yellow reverse
        local = yellow
        remote = green
    [color "diff"]
        meta = yellow bold
        frag = magenta bold
        old = red bold
        new = green bold
        whitespace = red reverse
    [color "status"]
        added = yellow
        changed = green
        untracked = cyan
[merge]
	tool = opendiff
[diff]
	tool = opendiff
[difftool]
	prompt = false
[mergetool]
	prompt = false
